# QA Gate 2 — Story 52-7 : XP bar animée

**Date** : 2026-04-05
**Status** : PASS

## Edge cases

| Scenario | Résultat |
|----------|----------|
| xp = 0 → fillPercent = 0 | ✅ `maxXp > 0 ? (xp/maxXp)*100 : 0` |
| xp > maxXp → clampé à 100% | ✅ `Math.min(100, ...)` |
| maxXp = 0 (cas légende) → 100% ou 0 | ✅ `maxXp > 0 ? ... : 0` |
| `animated=false` → setValue direct sans animation | ✅ |
| `Animated.Value` re-initialise si fillPercent change | ✅ `useEffect([fillPercent])` |
| Jalons gold quand prochain jalon atteint | ✅ `isNextMilestone = pct > fill && (pct-25) <= fill` |
| `joueurForTier=null` quand child absent | N/A — XPBar est dans tab Académie, `child` garanti non-null |

## Blockers : aucun
## Warnings : aucun
