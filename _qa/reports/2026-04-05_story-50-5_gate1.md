# QA Gate 1 — Story 50-5 : Dashboard Live Activity Feed
Date: 2026-04-05

## Périmètre
- `aureak/apps/web/app/(admin)/dashboard/page.tsx`
- `aureak/packages/api-client/src/admin/dashboard.ts`
- `aureak/packages/api-client/src/index.ts`

## Checklist BLOCKER

| Check | Statut | Détail |
|-------|--------|--------|
| `.unsubscribe()` dans useEffect return | ✅ PASS | Ligne 1065 : `return () => { channel.unsubscribe() }` |
| Pas de `setLoading(false)` hors finally | ✅ PASS | Aucun loading state sur activity feed (pattern simplifié justifié) |
| Channel name unique | ✅ PASS | `'dashboard-activity'` — unique dans le projet |

## Checklist WARNING

| Check | Statut | Détail |
|-------|--------|--------|
| console guards (`NODE_ENV !== 'production'`) | ✅ PASS | Tous les `console.error` dans dashboard.ts et page.tsx sont gardés |
| Catch silencieux | ✅ PASS | Aucun `catch(() => {})` |
| Accès Supabase direct dans apps/web | ✅ PASS | `supabase` importé depuis `@aureak/api-client` uniquement |

## TypeScript
```
npx tsc --noEmit → 0 erreurs
```

## ACs vérifiés en revue de code

- AC1 : Layout 2 colonnes implémenté via `.page-layout` (flex) + `.main-col` (flex:1) + `.aside-col` (width:280px)
- AC1 mobile : `@media (max-width: 1024px)` → flex-direction:column, aside-col width:100%
- AC2 : `fetchActivityFeed()` charge 5 présences + 5 joueurs, merge trié par date, limit 10
- AC3 : Types `presence`, `new_player`, `badge` (badge ignoré car table non vérifiée)
- AC4 : Channel `'dashboard-activity'` souscrit sur `attendance_records` INSERT, prepend + slice(0,20), unsubscribe au cleanup
- AC5 : `relativeTime()` avec seuils À l'instant/min/h/j ; timer minute via `setTickMinute`
- AC6 : Feed items avec icône/nom/description/timestamp ; surbrillance `.feed-item-new` ; scrollbar masquée
- AC7 : Sur `CHANNEL_ERROR`, le feed affiche uniquement les données statiques — pas de crash

## Statut
GATE 1 : ✅ PASS — aucun BLOCKER
