# QA Gate 1 — Story 50-2 — KPI cards sparkline + delta

date: 2026-04-05
story: story-50-2-dashboard-kpi-cards-sparkline-delta
agent: Amelia (Developer)
gate: 1 (code review + QA scan)

---

## Fichiers modifiés

- `aureak/apps/web/app/(admin)/dashboard/page.tsx`

---

## AC Coverage

| AC | Description | Status |
|----|-------------|--------|
| AC1 | SparklineSVG présent sur chaque card avec sparkline | PASS |
| AC2 | Données simulées déterministes via `simulateSpark()` + TODO(50.x) | PASS |
| AC3 | DeltaPill calcule delta depuis data[] (▲/▼/— selon signe) | PASS |
| AC4 | KpiCard accepte `sparkline?: number[]`, rend SVG+Pill conditionnellement | PASS |
| AC5 | Polyline 1.5px + circle last-point radius 3 en couleur accent | PASS |
| AC6 | SVG height=36, width=100% | PASS |
| AC7 | Aucune lib externe — SVG natif uniquement | PASS |

---

## Tasks

| Task | Status |
|------|--------|
| T1 — SparklineSVG (normalisation, coordonnées, polyline, circle) | DONE |
| T2 — DeltaPill (delta calculé, couleurs, fond semi-transparent) | DONE |
| T3 — KpiCard prop sparkline + rendu conditionnel | DONE |
| T4 — simulateSpark + 6 sparks (sessions/attendance/mastery/children/coaches/groups) | DONE |
| T5 — QA scan (no deps, no regression) | DONE |

---

## QA Scan Résultats

### BLOCKER checks

- `setLoading(false)` / `setLoadingCounts(false)` / `setResolving(null)` : tous dans `finally` — PASS
- Console guards : zéro `console.` sans guard `NODE_ENV` — PASS
- Silent catches : zéro `catch(() => {})` — PASS
- Hex hardcodés : zéro nouvelle couleur hex inline — PASS
- Lib externe : zéro recharts/victory/d3 — PASS

### Edge cases couverts (Bug Hunter)

- `SparklineSVG` : guard `!values || values.length < 2` → return null si données vides
- `SparklineSVG` : `safeValues = values.map(v => isNaN(v) || !isFinite(v) ? 0 : v)` → NaN guard
- `DeltaPill` : guard `!data || data.length < 2` → return null
- `DeltaPill` : `first === 0 || isNaN(first)` → delta = 0 (division par zéro évitée)
- `DeltaPill` : `abs < 0.1` → affiche "— stable" en gris (cas delta nul)
- `simulateSpark` : `base = current || 1` → évite base = 0
- Cards sans sparkline (Implantations, Anomalies) : `sparkline` undefined → ni SVG ni Pill rendu

### Warnings (non-bloquants)

- W1: `rgba(76,175,80,0.10)` et `rgba(244,67,54,0.10)` dans DeltaPill — tokens manquants dans @aureak/theme (pré-existant story-49-5 W3)
- W2: `TERRAIN_GRADIENT` hex hardcodés (#1a472a/#2d6a4f) — pré-existant story-49-5
- W3: Cards coachs/groupes : sparkline simulée même si `loadingCounts=true` (valeur `null` → spark undefined, OK)

---

## TypeScript

`cd aureak && npx tsc --noEmit` → 0 erreur

---

## Verdict

Gate 1 : PASS
Warnings actifs : 3 (tous pré-existants ou cosmétiques)
