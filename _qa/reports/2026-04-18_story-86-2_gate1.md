# Gate 1 Report — Story 86.2 (Multi-role utilisateur + switch contexte)

Date: 2026-04-18
Agent: claude-opus-4-6
Story: `_bmad-output/implementation-artifacts/86-2-multi-role-utilisateur-switch-contexte.md`

---

## Code Review

### STOP checks (5 mandatory)

| Check | Result |
|-------|--------|
| Import Supabase direct hors api-client | OK — `useAuthStore.ts` importe `supabase` depuis `@aureak/api-client` (pre-existing pattern), nouvelles fonctions utilisent `listUserRoles`/`setActiveRoleApi` |
| Style hardcode (hex, px) | OK — couleurs via `colors.*` tokens, `radius.*` tokens. `rgba()` overlays = pattern etabli dans le codebase |
| RLS absent | OK — `ALTER TABLE public.user_roles ENABLE ROW LEVEL SECURITY` + 5 policies |
| TypeScript `any` | OK — aucun `any` detecte |
| AC manquants | OK — AC1-AC5 tous couverts |

### Issues trouvees

#### [BLOCKER] `useAuthStore.ts:145` — catch silencieux sans console guard

**Avant** : `.catch(() => { set(...) })` — erreur avalee silencieusement.
**Correction appliquee** : ajout `console.error` guarde par `NODE_ENV`.

#### [WARNING] `RoleSwitcher.tsx:139,147,218,251` — rgba() hardcode

4 occurrences de `rgba(255,255,255,...)` et `rgba(0,0,0,...)` pour les overlays/hover.
Pattern etabli dans le codebase (52 occurrences dans 15 fichiers `@aureak/ui`).
Non-bloquant — alignement via theme tokens recommande a terme.

#### [INFO] `user-roles.ts:109-125` — physical DELETE sur table association

`removeUserRole()` fait un DELETE physique. Coherent avec le pattern projet pour les tables d'association (join tables) — cf. `methodology.ts`, `stages.ts`, `clubs.ts`, etc.

---

## Migration Validator — 00149_create_user_roles.sql

### STOP checks

| Check | Result |
|-------|--------|
| Numero sequence | OK — 00149 suit 00148 |
| DROP sans justification | OK — aucun DROP |
| RLS absent | OK — RLS active + 5 policies |
| Desync DB <> TS | OK — `UserRoleAssignment` dans entities.ts correspond |
| ALTER incompatible | OK — aucun ALTER |

### Checklist

| Check | Result |
|-------|--------|
| `CREATE TABLE IF NOT EXISTS` | OK |
| `CREATE INDEX IF NOT EXISTS` | OK |
| `INSERT ... ON CONFLICT DO NOTHING` | OK |
| FK references tables existantes | OK — `auth.users(id)`, `public.profiles` |
| `ON DELETE` explicite | OK — `CASCADE` |
| UNIQUE coherent | OK — `(user_id, role)` |
| Index sur colonnes filtrees | OK — `idx_user_roles_user_id` |

### Sync DB <> TypeScript

| Colonne DB | Type DB | Equivalent TS | Sync |
|------------|---------|---------------|------|
| id | UUID PK | id: string | OK |
| user_id | UUID NOT NULL | userId: string | OK |
| role | user_role NOT NULL | role: UserRole | OK |
| is_primary | BOOLEAN NOT NULL | isPrimary: boolean | OK |
| created_at | TIMESTAMPTZ NOT NULL | createdAt: string | OK |

### RLS Policies

| Table | Policy | Role | Type | Condition |
|-------|--------|------|------|-----------|
| user_roles | user_roles_select_own | authenticated | SELECT | auth.uid() = user_id |
| user_roles | user_roles_select_admin | admin | SELECT | profiles.user_role = 'admin' |
| user_roles | user_roles_insert_admin | admin | INSERT | profiles.user_role = 'admin' |
| user_roles | user_roles_update_admin | admin | UPDATE | profiles.user_role = 'admin' |
| user_roles | user_roles_delete_admin | admin | DELETE | profiles.user_role = 'admin' |

---

## Security Audit

### STOP checks

| Check | Result |
|-------|--------|
| RLS manquant | OK |
| Politique USING(true) | OK — aucune |
| Secret hardcode | OK — aucun |
| JWT non verifie | N/A — pas d'Edge Function |
| Cross-tenant | OK — filtre par auth.uid() |

### Matrice d'acces

| Table/Endpoint | admin | coach | parent | child | club |
|----------------|-------|-------|--------|-------|------|
| user_roles (SELECT own) | R | R | R | R | R |
| user_roles (SELECT all) | R | - | - | - | - |
| user_roles (INSERT) | C | - | - | - | - |
| user_roles (UPDATE) | U | - | - | - | - |
| user_roles (DELETE) | D | - | - | - | - |

### Observations

- Le `setActiveRole` dans api-client met a jour `profiles.user_role` pour backward compat — coherent avec le design note de la story.
- Le store Zustand note explicitement que `role` est UI-only, pas source d'autorite securite (ligne 2-3).
- Persistance localStorage du role actif = ok, pas de donnee sensible (juste le nom du role).

---

## Corrections appliquees

1. `useAuthStore.ts:145` — ajout console guard sur catch silencieux

## Verdict

**PASS** — 1 correction, 0 blockers non corrigibles.
