# Gate 1 Report — Story 34.3 : Redesign Entraînements v2 + Table Exercices

**Date** : 2026-04-08  
**Story** : `_bmad-output/implementation-artifacts/34-3-redesign-entrainements-table-exercices.md`  
**Status story** : done  
**Reviewer** : pipeline-gate1 (autonome)

---

## Fichiers analysés

| Fichier | Type |
|---------|------|
| `supabase/migrations/00141_create_methodology_exercises.sql` | Migration |
| `aureak/packages/types/src/entities.ts` | Types TS |
| `aureak/packages/api-client/src/methodology.ts` | API client |
| `aureak/packages/api-client/src/index.ts` | Export |
| `aureak/apps/web/app/(admin)/methodologie/seances/index.tsx` | UI |

---

## 1. Code Review

### STOP — 5 checks prioritaires

| Check | Résultat |
|-------|----------|
| Import Supabase direct hors api-client | PASS — aucun `createClient` dans apps/ |
| Style hardcodé (#hex) | PASS — aucune couleur hex brute |
| RLS absent sur nouvelle table | PASS — `ENABLE ROW LEVEL SECURITY` présent |
| TypeScript `any` nu | PASS — aucun `any` non justifié |
| Acceptance criteria manquants | PASS — AC 1-9 tous couverts |

### Issues détectées

**[WARNING] — Valeurs numériques inline non-tokenisées** (`seances/index.tsx`)

Plusieurs valeurs numériques sont utilisées directement sans passer par les tokens `space.*` ou `radius.*` :

- Ligne 247 : `fontSize: 12` (inline dans dropdown — hors `StyleSheet`)
- Ligne 523 : `maxWidth: 1200` — le token `layout.containerMaxWidth` est `1152`, valeur discordante
- Ligne 526 : `gap: 12` — utiliser `space.sm` (8) ou définir un token intermédiaire
- Lignes 529, 638 : `paddingVertical: 8`, `paddingVertical: 8` — `space.sm` existe
- Ligne 529 : `borderRadius: 8` — utiliser `radius.xs` (6) ou `radius.card` (16)
- Ligne 595 : `paddingBottom: 8` — `space.sm` disponible
- Ligne 629 : `borderRadius: 6` — `radius.xs` = 6, valeur identique mais à tokeniser
- Ligne 694 : `width: 8, height: 8, borderRadius: 4` — statusDot hardcodé

> Note : Ce pattern est présent dans d'autres pages de la codebase — niveau WARNING, pas BLOCKER dans ce contexte projet.

**[WARNING] — `maxWidth: 1200` discordant** (`seances/index.tsx:523`)

Le token `layout.containerMaxWidth` est `1152`. La page utilise `1200`. Mineur mais incohérent.

**[INFO] — Console guard dupliqué** (`methodology.ts:505`)

Ligne 505 : double `if (process.env.NODE_ENV !== 'production')` — guard imbriqué sans nécessité. Déjà présent dans d'autres fonctions du fichier (pattern copié/collé). Pas un BLOCKER.

**[INFO] — Inline styles dans sous-composants** (`seances/index.tsx:306, 400, 439`)

Quelques `style={{ color: colors.text.muted, fontStyle: 'italic', fontSize: 13 }}` inline pourraient être extraits dans `StyleSheet.create`. Mineur.

---

## 2. Migration Validator — 00141

### STOP — 5 checks prioritaires

| Check | Résultat |
|-------|----------|
| Numéro séquence correct (00140 → 00141) | PASS |
| DROP TABLE/COLUMN non justifié | PASS — aucun DROP |
| RLS absent | PASS — activé avec 4 policies |
| Désync DB ↔ TS | PASS — voir tableau ci-dessous |
| ALTER incompatible | PASS — aucun ALTER sur table existante |

### Tableau DB ↔ TypeScript

| Colonne DB | Type DB | Champ TS | Type TS | Sync |
|------------|---------|----------|---------|------|
| `id` | UUID NOT NULL | `id` | `string` | ✅ |
| `tenant_id` | UUID NOT NULL | `tenantId` | `string` | ✅ |
| `method` | TEXT NOT NULL | `method` | `MethodologyMethod` | ✅ |
| `context_type` | TEXT NOT NULL DEFAULT 'academie' | `contextType` | `MethodologyContextType` | ✅ |
| `title` | TEXT NOT NULL | `title` | `string` | ✅ |
| `training_ref` | INT nullable | `trainingRef` | `number \| null` | ✅ |
| `description` | TEXT nullable | `description` | `string \| null` | ✅ |
| `pdf_url` | TEXT nullable | `pdfUrl` | `string \| null` | ✅ |
| `is_active` | BOOLEAN NOT NULL | `isActive` | `boolean` | ✅ |
| `created_at` | TIMESTAMPTZ NOT NULL | `createdAt` | `string` | ✅ |
| `updated_at` | TIMESTAMPTZ NOT NULL | `updatedAt` | `string` | ✅ |
| `deleted_at` | TIMESTAMPTZ nullable | `deletedAt` | `string \| null` | ✅ |

### Politiques RLS

| Table | Politique | Condition |
|-------|-----------|-----------|
| `methodology_exercises` | `tenant_isolation_select` (SELECT) | `tenant_id = profiles.tenant_id WHERE id = auth.uid()` |
| `methodology_exercises` | `tenant_isolation_insert` (INSERT) | idem |
| `methodology_exercises` | `tenant_isolation_update` (UPDATE) | idem |
| `methodology_exercises` | `tenant_isolation_delete` (DELETE) | idem |

### Idempotence

- `CREATE TABLE IF NOT EXISTS` : PASS
- `CREATE INDEX IF NOT EXISTS` : PASS (3 index créés)
- FK `tenants(id) ON DELETE CASCADE` : PASS
- `deleted_at` présent : PASS

### [WARNING] — CHECK constraint absent sur `method`

La colonne `method TEXT NOT NULL` n'a pas de `CHECK (method IN (...))` contrairement à `context_type` qui a un CHECK. C'est cohérent avec `methodology_sessions` (qui n'a pas non plus de CHECK sur `method`) — WARNING de cohérence, pas BLOCKER.

---

## 3. Security Auditor

### STOP — 5 checks prioritaires

| Check | Résultat |
|-------|----------|
| RLS manquant | PASS — activé |
| Politique USING(true) | PASS — aucune |
| Secret hardcodé | PASS — aucun |
| JWT non vérifié (edge function) | N/A — aucune edge function dans cette story |
| Cross-tenant (requête sans tenant_id) | PASS — toutes les policies filtrent par tenant_id |

### Matrice d'accès — `methodology_exercises`

| Table | admin | coach | parent | child | club_partner | club_common |
|-------|-------|-------|--------|-------|--------------|-------------|
| `methodology_exercises` | CRUD | R | - | - | - | - |

> Note : La RLS utilise le tenant_id du profil (`profiles.tenant_id`), donc l'isolation est correcte mais les policies ne distinguent pas admin vs coach. Cohérent avec le pattern des autres tables methodology_* — pas un BLOCKER.

---

## Résumé

| Catégorie | BLOCKERs | WARNINGs | INFOs |
|-----------|----------|----------|-------|
| Code Review | 0 | 2 | 2 |
| Migration | 0 | 1 | 0 |
| Security | 0 | 0 | 1 |
| **Total** | **0** | **3** | **3** |

### Corrections effectuées

Aucune correction automatique nécessaire — aucun BLOCKER identifié.

Les WARNINGs (valeurs numériques inline, maxWidth discordant, CHECK absent sur `method`) sont des patterns existants dans la codebase et n'empêchent pas le fonctionnement correct de la feature.

---

**Verdict final** : PASS
