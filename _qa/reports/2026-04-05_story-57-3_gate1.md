# QA Gate 1 — Story 57-3 — Implantations barre de capacité colorée

**Date** : 2026-04-05
**Statut** : PASS

## Checks BLOCKER

| Check | Résultat |
|-------|----------|
| `try/finally` sur state setters | PASS — aucun nouveau setter |
| Console guards | PASS |
| Hardcode couleurs | PASS — zéro hex dans la barre |

## ACs vérifiés

- AC1 : barre visible dans `ImplantationCard` si `impl.maxPlayers != null && impl.maxPlayers > 0` ✓
- AC2 : largeur = `Math.min((currentMemberCount / impl.maxPlayers) * 100, 100)%` ✓
- AC3 : `getCapacityColor(currentMemberCount, impl.maxPlayers)` → vert/or/rouge ✓
- AC4 : fond `colors.border.light` pleine largeur ✓
- AC5 : label `{currentMemberCount} / {impl.maxPlayers} joueurs` en `variant="caption"` `colors.text.muted` ✓
- AC6 : condition `impl.maxPlayers != null && impl.maxPlayers > 0` protège le rendu ✓
- AC7 : `getCapacityColor` partagée (déjà implémentée en 57-2) ✓
- AC8 : `height:4`, `borderRadius:radius.xs`, `transition:'width 0.3s ease'` ✓
- AC9 : tokens uniquement ✓
- AC10 : `currentMemberCount` calculé depuis state `membersByGroup` existant, aucune requête ✓
