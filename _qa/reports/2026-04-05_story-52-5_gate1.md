# QA Gate 1 — Story 52-5 : Filtres tier pills

**Date** : 2026-04-05
**Status** : PASS

## Checks

| Check | Résultat |
|-------|----------|
| try/finally sur setLoading | N/A (pas de nouveaux setLoading ajoutés) |
| console guards (NODE_ENV) | N/A (aucun nouveau console.error) |
| Tokens @aureak/theme uniquement | ✅ — `colors.light.*`, `colors.border.*`, `colors.text.*` utilisés dans TIER_PILLS_CONFIG |
| Import `computePlayerTier` depuis `@aureak/business-logic` | ✅ |
| Import `PlayerTier` depuis `@aureak/types` | ✅ |
| `selectedTiers` persisté localStorage | ✅ — clé `aureak_players_tier_filter` |
| Multi-sélection | ✅ — `handleToggleTier` toggle individuel |
| `Tous` = reset | ✅ — `handleToggleTier('Tous')` → `setSelectedTiers([])` |
| `filteredByTier` utilisé dans rendu | ✅ — les 3 branches de rendu (loading/galerie/liste) |
| Compteurs tier | ✅ — `tierCounts` calculé sur `joueurs` (données après filtres serveur) |
| `tsc --noEmit` | ✅ 0 erreur |

## AC vérifiés
- AC1 Row de pills : ✅ ScrollView horizontal
- AC2 Couleur par tier : ✅ TIER_PILLS_CONFIG conforme aux specs
- AC3 Compteur par pill : ✅ tierCounts[tier]
- AC4 Multi-sélection : ✅
- AC5 Indépendance filtres : ✅ (filteredByTier s'applique après filtres serveur)
- AC6 Persistance localStorage : ✅
- AC7 Visibilité galerie + liste : ✅
