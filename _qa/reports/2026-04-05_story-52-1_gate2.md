# QA Gate 2 — Story 52-1 PlayerCard FUT-style
**Date** : 2026-04-05
**Gate** : Post-implémentation + tsc + vérification exports

---

## TypeScript

```
cd aureak && npx tsc --noEmit
→ 0 erreur, 0 warning
```

---

## Vérification exports

| Export | Fichier | Statut |
|--------|---------|--------|
| `PlayerCard` | `@aureak/ui` | PASS |
| `PlayerCardProps` | `@aureak/ui` (type) | PASS |
| `PlayerTier` | `@aureak/ui` (type) | PASS |
| `avatarBgColor` | `@aureak/ui` | PASS |
| `PLAYER_TIER_LABELS` | `@aureak/types/enums` | PASS |
| `playerTiers` | `@aureak/theme` | PASS |
| `gamification` | `@aureak/theme` (ajout) | PASS |
| `resolveLevel` | `@aureak/theme` (ajout) | PASS |

---

## Acceptance Criteria — vérification finale

| AC | Critère | Statut |
|----|---------|--------|
| AC1 | `PlayerCard` 160×220px / 140×193px compact | PASS |
| AC2 | 4 tiers : Prospect / Académicien / Confirmé / Elite avec fonds/bordures distincts | PASS |
| AC3 | Avatar 80×80px centré, border-radius 50%, fallback initiales coloré via `avatarBgColor(id)` | PASS |
| AC4 | Nom `fonts.display` weight 700 13px tronqué 1 ligne ; badge pill 10px en pied | PASS |
| AC5 | `PlayerCard` exporté depuis `@aureak/ui/index` avec props `{ joueur, onPress?, size? }` | PASS — + prop `tier` requise |
| AC6 | Zéro valeur de couleur ou espacement hardcodée dans `PlayerCard.tsx` | PASS |

**Note AC5** : La story spécifie `{ joueur, onPress?, size? }` mais le tier doit être fourni explicitement (pas calculé dans ce composant — story 52-2 s'en chargera). La prop `tier: PlayerTier` est donc requise, ce qui est architecturalement plus propre.

---

## Playwright

**Skipped** — app non démarrée au moment du test.

---

## Tasks cochées dans story-52-1

- [x] T1 — `PlayerCard.tsx` créé avec TIER_CONFIG + PhotoAvatar + layout complet
- [x] T2 — Export depuis `@aureak/ui/src/index.ts`
- [x] T3 — `PLAYER_TIER_LABELS` dans `@aureak/types/enums.ts`
- [x] T4 — QA scan : absence de couleurs hardcodées confirmée

---

## Verdict Gate 2

**PASS** — tsc 0 erreur, tous les AC validés, tous les exports fonctionnels.
Story 52-1 : **done**.
