# QA Gate 1 — Story 52-8 : Card evolution animation tier upgrade

**Date** : 2026-04-05
**Status** : PASS

## Checks

| Check | Résultat |
|-------|----------|
| `prevTierRef` avant sauvegarde | ✅ mis à jour avant `setSavingEdit(true)` |
| `flashOpacity` Animated.Value | ✅ `useRef(new Animated.Value(0))` |
| `triggerLevelUpAnimation` | ✅ CSS flip + Animated flash + toast |
| CSS `@keyframes tierFlip` injecté | ✅ `document.createElement('style')` singleton `aureak-tierflip-styles` |
| Flash overlay gold | ✅ `Animated.View` `position:absolute` dans header |
| Toast après 1s | ✅ `setTimeout(() => toast.success(...), 1000)` |
| Détection upgrade uniquement | ✅ `TIER_ORDER[newTier] > TIER_ORDER[prevTier]` |
| Platform.OS === 'web' guard pour CSS | ✅ |
| try/finally sur `setSavingEdit` | ✅ `finally { setSavingEdit(false) }` |
| `tsc --noEmit` | ✅ 0 erreur |

## AC vérifiés
- AC1 Détection upgrade : ✅ TIER_ORDER comparaison
- AC2 Animation flip CSS : ✅ via `@keyframes tierFlip` + `.tier-flip-anim`
- AC3 Flash or : ✅ `Animated.sequence` opacity 0→0.8→0
- AC4 Toast niveau up : ✅ `⭐ Niveau up ! ${name} est maintenant ${newTier}`
- AC5 Déclencheur upgrade uniquement : ✅
- AC6 Fallback mobile (toast seul) : ✅ `Platform.OS === 'web'` guard sur CSS
- AC7 Une seule fois par session : ✅ déclenché uniquement dans `saveEdit` post-success
