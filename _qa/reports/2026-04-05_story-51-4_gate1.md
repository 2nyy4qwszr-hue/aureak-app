# QA Gate 1 — Story 51.4 : Notification badges sidebar

**Date** : 2026-04-05
**Story** : 51.4 — Badges sidebar présences (rouge) + séances (or)
**Statut gate 1** : PASS

## Fichiers analysés

- `aureak/packages/types/src/entities.ts` — interface `NavBadgeCounts`
- `aureak/packages/api-client/src/admin/dashboard.ts` — `getNavBadgeCounts()`
- `aureak/packages/api-client/src/index.ts` — exports
- `aureak/apps/web/components/NavBadge.tsx` — composant badge (nouveau)
- `aureak/apps/web/app/(admin)/_layout.tsx` — intégration polling + rendu

## Checks

### BLOCKER — State setter sans finally

```bash
grep -n "setLoading(false)|setSaving(false)|setCreating(false)" <fichiers>
```

Résultat : **PASS** — Aucun state setter de chargement UI (`navBadges` n'est pas un état de loading/saving, pas de try/finally requis selon story task 5.2).

### WARNING — Console non guardé

Tous les `console.error` sont encadrés par `if (process.env.NODE_ENV !== 'production')` :
- `_layout.tsx` ligne 170 : `[AdminLayout] getNavBadgeCounts error` — gardé ✓
- `dashboard.ts` lignes 291-296 : erreurs unvalidated + upcoming24h — gardés ✓

Résultat : **PASS**

### WARNING — Catch silencieux

Résultat : **PASS** — Aucun `catch(() => {})` dans les fichiers modifiés.

### BLOCKER — Polling cleanup

Résultat : **PASS** — L'effet `useEffect` pour navBadges :
- Déclare `cancelled = true` dans le return de cleanup
- Appelle `clearInterval(intervalId)` dans le return de cleanup
- Respecte le pattern identique à l'effet activeSessions existant

## Bugs hunter — vérifications spécifiques

### Bug 1 : Badges 0 cachés
- `NavBadge` retourne `null` si `count === 0` ou `count` absent — **PASS**
- Dans `_layout.tsx`, le rendu badge présences est conditionnel : `navBadges.presencesUnvalidated > 0` — **PASS**
- Badge or : conditionnel `navBadges?.sessionsUpcoming24h` (boolean) — **PASS**

### Bug 2 : Valeurs > 99
- `NavBadge` : `count > 99 ? '99+' : String(count)` — **PASS**

### Bug 3 : Déduplification des sessions (plusieurs attendees NULL pour même session)
- `getNavBadgeCounts()` utilise un `Set` sur les IDs sessions retournés — **PASS**

### Bug 4 : Course condition polling (cancelled flag)
- `cancelled` flag empêche `setNavBadges` après unmount — **PASS**

### Bug 5 : navBadges null au premier rendu
- Les conditions utilisent `navBadges && navBadges.presencesUnvalidated > 0` et `navBadges?.sessionsUpcoming24h` — **PASS** (null-safe)

## TypeScript

```bash
cd aureak && npx tsc --noEmit
```

Résultat : **PASS** — 0 erreur

## AC Coverage

| AC | Statut |
|----|--------|
| AC1 — Badge rouge présences avec compteur | ✓ |
| AC2 — Badge or séances point | ✓ |
| AC3 — Badge visible mode collapsed | ✓ (même code, position relative sur icône) |
| AC4 — Polling 5 min + cleanup unmount | ✓ |
| AC5 — try/catch + console guard | ✓ |
| AC6 — Taille 16×16 chiffré / 8×8 point | ✓ |
| AC7 — Aucun badge si 0 | ✓ |
