# QA Gate 1 — Story 50.3 : Dashboard Prochaine Séance Countdown Tile
Date: 2026-04-05

## Scope
- `aureak/packages/api-client/src/sessions/sessions.ts` — ajout `listNextSessionForDashboard`
- `aureak/packages/api-client/src/index.ts` — export `listNextSessionForDashboard` + type `UpcomingSessionRow`
- `aureak/apps/web/app/(admin)/dashboard/page.tsx` — `CountdownTile` + state + intégration bento

## Checks

### BLOCKER — try/finally sur setters de chargement
- `setLoadingUpcoming(false)` : ligne 846, dans bloc `finally` ✅ PASS

### WARNING — console guards
- `console.error('[dashboard] listNextSessionForDashboard error:', error)` : guardé par `process.env.NODE_ENV !== 'production'` ✅ PASS
- Aucun `console.` non-guardé dans les fichiers modifiés ✅ PASS

### WARNING — catch silencieux
- Aucun `catch(() => {})` détecté ✅ PASS

### Règle clearInterval
- `return () => clearInterval(timer)` dans le useEffect du CountdownTile (ligne 630) ✅ PASS

### Tokens design
- Fond : `linear-gradient(135deg, #2A2827 0%, #1A1A1A 100%)` (gradient premium défini dans AC6) ✅ OK (valeur spec)
- `borderTop: 3px solid colors.accent.gold` ✅ token
- Countdown couleur : `colors.accent.gold` ✅ token
- Label couleur : `colors.accent.goldLight` / `colors.text.muted` ✅ tokens
- Texte groupe : `colors.text.secondary` ✅ token
- État "En cours" : `colors.status.present` ✅ token
- `borderRadius: radius.card` ✅ token

### TypeScript
- `npx tsc --noEmit` → 0 erreur ✅ PASS

## Résultat : GATE 1 PASS
