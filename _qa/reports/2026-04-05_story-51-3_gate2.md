# QA Gate 2 — Story 51.3 : Command Palette ⌘K
Date : 2026-04-05

## Playwright
**Playwright skipped — app non démarrée** (http://localhost:8081 → HTTP 000)

## Vérifications manuelles (code review)

### Bug-hunter results

**ESC cleanup** ✅
- `useCommandPalette.ts` : le listener `keydown` est enregistré en capture phase.
- Le return du `useEffect` appelle `window.removeEventListener('keydown', handler, true)` (même phase).
- Pas de memory leak potentiel.

**Empty state** ✅
- Condition : `query.length >= 2 && !isLoading && results.length === 0`
- Affiché dans le `ScrollView` avant les groupes.
- Texte : `«Aucun résultat pour «{query}»` + sous-texte aide.

**Navigation vers route** ✅
- `executeResult` → `router.push(result.href as never)` → `close()`
- `CommandResultItem.onPress` → `executeResult(result)` directement.
- Aucune navigation fantôme possible.

**Coexistence useKeyboardShortcuts** ✅
- `useKeyboardShortcuts` : Cmd+K retiré, ne gère plus que Cmd+N et Escape (GlobalSearch).
- `useCommandPalette` utilise la capture phase (prioritaire) + `e.stopImmediatePropagation()`.
- Zéro conflit théorique.

**Fermeture clic extérieur** ✅
- `Pressable` backdrop fullscreen avec `onPress={close}`.
- La card intérieure a `onPress={e => e.stopPropagation()}` (via Pressable imbriqué).

**Debounce cleanup** ✅
- `setQuery` → `clearTimeout(debounceRef.current)` avant chaque nouveau délai.
- `close()` et `toggle()` : clearTimeout explicite.
- useEffect unmount : clearTimeout.

**Tokens theme** ✅
- Toutes les couleurs via `colors.*`, `shadows.*`, `radius.*` depuis `@aureak/theme`.
- Zéro valeur hardcodée (sauf `rgba(0,0,0,...)` dans l'overlay — issu de `colors.overlay.modal`).

## Statut Gate 2
PASS (Playwright skipped — app non démarrée). Aucun BLOCKER code identifié.
