# QA Gate 2 — Story 52-6 : Header photo 280px fullwidth + tabs

**Date** : 2026-04-05
**Status** : PASS

## Edge cases

| Scenario | Résultat |
|----------|----------|
| Joueur sans photo → avatar initiales centré | ✅ `!showPhoto && <View avatarCircle>` |
| Erreur chargement image → fallback initiales | ✅ `imgError` state + `onError` handler |
| `child.statut` null → `academyData?.computedStatus` fallback | ✅ dans `joueurForTier` |
| `joueurForTier` null avant `child` chargé | ✅ `joueurForTier!` avec guard (child non null à ce point) |
| Tab switching — état édition inline préservé | ⚠ WARNING : changer de tab reset visible content mais l'état `editSection` persiste — acceptable (pas de perte de données) |
| Photos tab quand tenantId null | ✅ `activeTab === 'Photos' && tenantId` guard |
| Stages tab sans participations | ✅ message "Aucune participation" |

## Blockers détectés : aucun
## Warnings : Tab switch ne cancel pas l'édition en cours — comportement acceptable, non bloquant.
