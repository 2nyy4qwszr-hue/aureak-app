# QA Gate 2 — Story 50-2 — KPI cards sparkline + delta

date: 2026-04-05
story: story-50-2-dashboard-kpi-cards-sparkline-delta
agent: Amelia (Developer)
gate: 2 (pre-deploy — bug hunter + edge cases)

---

## Synthèse Gate 1

Gate 1 : PASS — 0 BLOCKER, 3 warnings pré-existants

---

## Bug Hunter — Edge Cases

### Checklist BLOCKER (5 points critiques)

1. `.data.field` sans optional chaining → N/A (cette story ne fait pas d'appels Supabase)
2. Double-soumission → N/A (pas de formulaire submit)
3. try/catch silencieux → PASS (aucun dans les nouvelles fonctions)
4. Erreur Supabase ignorée → N/A (pas de nouveaux appels API)
5. Mutation sans invalidation → N/A (pas de TanStack Query)

### Edge cases métier

- **sparkline vide** : `SparklineSVG` retourne `null` si `data.length < 2` → aucun rendu cassé
- **sparkline avec NaN** : `safeValues` remplace NaN/Infinity par 0 avant normalisation
- **delta null** : `DeltaPill` affiche "— stable" si `abs < 0.1` (cas current=0 ou variation < 0.1%)
- **childrenTotal = null** (pendant loadingCounts) : `sparkChildren = undefined` → card sans sparkline → PASS
- **totalSessions = 0** : `sparkSessions = undefined` → pas de sparkline → PASS
- **avgAttendance = null** (pas de stats) : `sparkAttendance = undefined` → PASS
- **Cards sans sparkline** (Implantations, Anomalies) : prop `sparkline` non passée → ni SVG ni DeltaPill → PASS (AC4 no-regression)

### Régression

- Ancienne `DeltaPill({ value, positive })` remplacée par `DeltaPill({ data })` — les anciens appels avec `delta` prop supprimés dans `KpiCard` — PASS
- `KpiCard` sans la prop `delta` (supprimée du type) → les 8 cartes existantes compilent sans erreur — PASS
- `SparklineSVG` : props renommées de `{values}` → `{values}` (inchangé) — PASS

---

## Playwright

App non démarrée au moment du commit — Playwright skipped (conforme aux instructions CLAUDE.md).

---

## Warnings non résolus des agents précédents

| Agent | Warning | Résolu? | Action requise |
|-------|---------|---------|----------------|
| story-49-5 W3 | rgba() hardcodés DeltaPill | Partiellement — DeltaPill refactorisée mais rgba() conservés | Extraire tokens colors.status.presentBg/absentBg (future) |
| story-49-5 W5 | SparklineSVG sans guard NaN | Résolu dans story-50-2 | — |
| story-50-1 W2 | rgba() hardcodés DeltaPill/NextSessionTile | Pré-existant — DeltaPill refactorisée mais rgba() conservés | Future |

---

## Verdict Final Story 50-2

Gate 1 : PASS
Gate 2 : PASS
Warnings actifs : 2 (rgba() hardcodés pré-existants — tech debt cosmétique)

Recommandation : PRÊT POUR PRODUCTION
