# QA Gate 2 — Story 52-5 : Filtres tier pills

**Date** : 2026-04-05
**Status** : PASS

## Edge cases

| Scenario | Résultat |
|----------|----------|
| Liste vide → tierCounts tous à 0 | ✅ `joueurs.forEach` sur array vide = counts restent à 0 |
| localStorage non disponible (SSR) | ✅ `typeof localStorage === 'undefined'` guard dans `loadTierFilter()` |
| JSON.parse malformé dans localStorage | ✅ try/catch dans `loadTierFilter()`, retourne `[]` |
| Tier inconnu retourné par `computePlayerTier` | ✅ `counts[tier]++` — les tiers sont exhaustifs (PlayerTier union type) |
| Sélection de tous les tiers simultanément | ✅ = filtre non-Tous actif, comportement cohérent |
| `selectedTiers` vide = afficher tous | ✅ `filteredByTier` retourne `joueurs` si `selectedTiers.length === 0` |
| Persistance cross-session | ✅ `loadTierFilter()` initialisé depuis localStorage |

## Blockers détectés : aucun
## Warnings : aucun
