# QA Gate 1 — Story 57-1 — Implantations drag & drop photo + preview + compression

**Date** : 2026-04-05
**Statut** : PASS

## Checks BLOCKER

| Check | Résultat |
|-------|----------|
| `setUploadingPhoto(null)` dans `finally` | PASS — ligne 825, bloc `finally` confirmé |
| Pas de `setLoading` inline sans `finally` | PASS |
| Console guards `NODE_ENV !== 'production'` | PASS — tous les `console.error` sont sous guard |
| Catch silencieux `catch(() => {})` | PASS — aucun |

## Checks WARNING

| Check | Résultat |
|-------|----------|
| Tokens theme uniquement | PASS — `colors.accent.gold`, `radius.card`, `colors.accent.red` |
| Hardcode couleurs | PASS — zéro hex hardcodé (overlay rgba = sémantique) |
| Import Supabase direct | PASS — via `@aureak/api-client` uniquement |

## ACs vérifiés

- AC1 : `onDragOver` avec `e.preventDefault()` + state `isDragOver` → overlay gold ✓
- AC2 : `e.dataTransfer.files?.[0]` dans `handleDrop` ✓
- AC3 : `compressImage(file, 800)` avant upload ✓
- AC4 : `URL.createObjectURL(file)` → `previewUrls[impl.id]` → affiché dans `<Image>` ✓
- AC5 : barre fine `height:3` or en bas de coverContainer quand `uploadingPhoto` ✓
- AC6 : preview retirée + `photoErrors[implId]` affiché en rouge ✓
- AC7 : vérification `file.type.startsWith('image/')` ✓
- AC8 : bouton clic et drop → même handler `handlePhotoUpload` ✓
- AC9 : zéro hardcode — tokens theme ✓
- AC10 : tous `console.error` guardés ✓
