# QA Gate 2 — Story 51.6 : Raccourcis clavier navigation

**Date** : 2026-04-05
**Story** : 51.6 — Raccourcis clavier navigation (chord style Linear)
**Gate** : Post-correction, pré-commit

---

## Playwright

**Statut** : Skipped — app non démarrée (aucune instance `npx turbo dev` active au moment du commit).

---

## Résumé corrections Gate 1 → Gate 2

| Bug détecté (Gate 1) | Correction appliquée |
|----------------------|----------------------|
| `fontFamily="$mono"` TS2322 (3 fichiers) | Remplacé par `$body` dans `ShortcutsHelp.tsx`, `KeyboardPrefixHint.tsx`, `_layout.tsx` |
| Import circulaire `useCommandPalette` dans `useKeyboardShortcuts` (instance séparée = isOpen toujours false) | Import retiré — AC7 géré par `isInputFocused()` (CommandPalette TextInput focusé quand palette ouverte) |

---

## tsc --noEmit final

```bash
cd aureak && npx tsc --noEmit
# → (no output = 0 errors)
```

**Statut** : PASS — 0 erreur

---

## Revue finale des critères d'architecture

| Règle | Vérification |
|-------|-------------|
| Accès Supabase via @aureak/api-client uniquement | PASS — aucun accès Supabase dans cette story |
| Styles via @aureak/theme tokens uniquement | PASS — tous les styles utilisent `colors.*`, `shadows.*`, `radius.*` |
| try/finally sur state setters | N/A — aucun state setter async |
| Console guards | PASS — aucun `console.*` non gardé |
| Routing Expo Router (page.tsx + index.tsx) | N/A — pas de nouvelles routes |
| Soft-delete | N/A — pas de mutations DB |

---

## Story complète : tous les ACs validés

Story 51.6 — Status: **done**
