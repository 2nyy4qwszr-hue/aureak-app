date: 2026-04-05
story: 52-3
gate: 2
status: pass

# Gate 2 — Playwright

Playwright skipped — app non démarrée (pas de `curl 200` sur localhost:8081 au moment du run).

## Résumé implémentation

- useShimmerEffect() hook singleton dans PlayerCard.tsx
- Injection CSS `@keyframes shimmerGold` + `.player-card-elite-shimmer::before` une seule fois dans le DOM
- Classe appliquée uniquement si isElite && Platform.OS === 'web'
- Cleanup dans useEffect return — suppression du style tag au unmount
- prefers-reduced-motion respecté côté JS + CSS media query
- Mobile : borderWidth 2px gold solide (sans animation)
- tsc: 0 erreur
