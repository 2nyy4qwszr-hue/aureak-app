# Gate 1 Report — Story 82.1 — Académie Coachs Layout Activités

**Date** : 2026-04-09
**Story** : `_bmad-output/implementation-artifacts/82-1-academie-coachs-layout-activites.md`
**Fichier analysé** : `aureak/apps/web/app/(admin)/academie/coachs/index.tsx`
**Reviewer** : pipeline-gate1 (autonome)

---

## Résumé

| Catégorie | Résultat |
|-----------|----------|
| BLOCKERs | 0 |
| WARNINGs | 0 |
| INFOs | 2 |
| Corrections appliquées | 0 |

---

## Check 5 points STOP (vérification immédiate)

| # | Règle | Statut |
|---|-------|--------|
| 1 | Import Supabase direct | ✅ Aucun — API via `@aureak/api-client` uniquement |
| 2 | Style hardcodé (hex, valeurs px brutes) | ✅ Aucun hex ni rgba. Les valeurs numériques brutes (`8`, `14`, `16`, `10`) sont celles prescrites par l'AC et présentes dans le fichier de référence `methodologie/seances/index.tsx` — pattern accepté dans le projet |
| 3 | RLS absent (migration) | N/A — pas de migration |
| 4 | TypeScript `any` nu | ✅ Aucun `any` non justifié. Les `as never` sont utilisés pour contourner les limitations de typages React Native + StyleSheet (pattern courant dans la codebase) |
| 5 | Acceptance criteria manquants | ✅ Tous les AC couverts (voir détail ci-dessous) |

---

## Vérification des Acceptance Criteria

### AC 1 — headerBlock ✅
- Titre `COACHS` : `fontSize: 24, fontWeight: '700', fontFamily: 'Montserrat', color: colors.text.dark` → ligne 301 ✅
- Bouton `+ Nouveau coach` : `backgroundColor: colors.accent.gold` → ligne 302 ✅
- Label bouton : `color: colors.text.dark, fontWeight: '700', fontSize: 13` → ligne 304 ✅

### AC 2 — StatCards ✅
- Row container : `flexDirection: 'row', gap: space.md, paddingHorizontal: space.lg, paddingVertical: space.md, flexWrap: 'wrap'` → lignes 307–313 ✅
- Chaque card : `backgroundColor: colors.light.surface, borderRadius: radius.card, padding: space.md, borderWidth: 1, borderColor: colors.border.divider, minWidth: 160, boxShadow: shadows.sm` → lignes 314–325 ✅
- Picto emoji + label uppercase 10px Montserrat 700 muted + count 28px 900 → lignes 326–340 ✅
- 4 cards : COACHS (dark), GRADE OR+ (gold), DIPLÔMÉS (muted), SCORE ACADÉMIE (gold) → lignes 134–167 ✅
- `goldPlusCount` calculé avec `['gold','platinum']` → lignes 99–101 ✅

### AC 3 — FiltresRow ✅
- Pill TOUS gauche : active/inactive correctement conditionnée `roleFilter === 'all'` → lignes 171–179 ✅
- SegmentedToggle COACH/ASSISTANT droite : pattern `toggleRow/toggleBtn/toggleBtnActive/toggleLabel/toggleLabelActive` → lignes 182–199 ✅
- `FilterChip` supprimé → aucune trace dans le fichier ✅
- Styles `chip.*` supprimés → aucune trace dans le fichier ✅

### AC 4 — CoachsTable ✅
- `tableWrapper` : `borderRadius: 10, borderWidth: 1, borderColor: colors.border.divider, overflow: 'hidden'` → lignes 383–388 ✅
- `tableHeader` : `paddingHorizontal: 16, paddingVertical: 10, backgroundColor: colors.light.muted, borderBottomWidth: 1, borderBottomColor: colors.border.divider` → lignes 389–397 ✅
- `thText` : `fontSize: 10, fontWeight: '700', fontFamily: 'Montserrat', color: colors.text.subtle, textTransform: 'uppercase', letterSpacing: 1` → lignes 398–405 ✅
- Lignes : `paddingHorizontal: 16, minHeight: 52` → lignes 406–413 ✅
- Zébré : `idx % 2 === 0 ? colors.light.surface : colors.light.muted` → ligne 226 ✅

### AC 5 — Pagination ✅
- `backgroundColor: colors.light.muted` → ligne 439 ✅
- `borderTopWidth: 1, borderTopColor: colors.border.divider` → lignes 440–441 ✅
- Boutons pagination : `backgroundColor: colors.light.surface, borderWidth: 1, borderColor: colors.border.light` → ligne 445 ✅

### AC 6 — QA ✅
- try/finally sur le loader : `setLoading(true)` ligne 66, `setLoading(false)` dans `finally` (lignes 84–86), avec guard `!cancelled` ✅
- console guard : ligne 83 → `if (process.env.NODE_ENV !== 'production') console.error(...)` ✅
- `FilterChip` + styles `chip.*` supprimés ✅
- Styles orphelins `stat.*` supprimés ✅

---

## Vérification Architecture

| Règle | Statut |
|-------|--------|
| Accès Supabase via `@aureak/api-client` uniquement | ✅ `listCoaches`, `getCoachCurrentGrade` depuis `@aureak/api-client` |
| Styles via tokens `@aureak/theme` | ✅ `colors`, `space`, `radius`, `shadows` importés et utilisés |
| Pas de `any` nu | ✅ |
| Routing index.tsx | ✅ Fichier `index.tsx` (pas `page.tsx`) → route directe, pattern valide |
| Enums constants | ✅ `CoachGradeLevel` utilisé comme Record key |

---

## Qualité du code

| Point | Statut |
|-------|--------|
| console.log debug oublié | ✅ Aucun |
| Code mort ou commenté sans raison | ✅ Un seul commentaire `// Note:` ligne 93–95 qui documente un comportement intentionnel (filtre ASSISTANT non fonctionnel) — justifié |
| Duplication évidente | ✅ Aucune |
| Gestion d'erreur critique | ✅ try/catch/finally complet |
| Non-null assertions `!` | ✅ Aucune |

---

## Points INFO (non bloquants)

### INFO 1 — Filtre ASSISTANT non fonctionnel (documenté intentionnellement)
**Fichier** : `index.tsx:96`
**Observation** : `const filtered = coaches.filter(() => roleFilter !== 'assistant')` — logique simplifiée, ASSISTANT retourne toujours vide.
**Contexte** : Commentaire explicatif présent lignes 93–95. Comportement intentionnel documenté en attendant une story future sur le champ `coachRole`.
**Action** : Aucune — comportement accepté et documenté.

### INFO 2 — `@ts-ignore web` sur `boxShadow`
**Fichier** : `index.tsx:323`
**Observation** : `// @ts-ignore web` sur la propriété `boxShadow` dans le StyleSheet.
**Contexte** : Pattern standard dans toute la codebase pour les propriétés CSS web non supportées par React Native types. Aucune action requise.

---

## Verdict

**PASS** — 0 BLOCKER, 0 WARNING, 2 INFO.
Aucune correction appliquée. Le code respecte intégralement les AC de la story 82.1, les règles d'architecture du projet, et les patterns try/finally + console guards.
