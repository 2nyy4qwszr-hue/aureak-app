# QA Gate 1 — Story 55-7 : Évaluations — Export scouting PDF

**Date** : 2026-04-05
**Status** : PASS (erreurs TS corrigées)

## Fichiers analysés

- `aureak/apps/web/utils/generateScoutingPDF.ts`
- `aureak/apps/web/components/pdf/ScoutingPDF.tsx`
- `aureak/apps/web/app/(admin)/children/[childId]/page.tsx`

## Corrections TypeScript appliquées

1. `ScoutingPDF.tsx` ligne 9 : `PlayerAxisAverage` importé depuis `@aureak/api-client` (pas `@aureak/types`) — il s'agit d'un type défini dans le module API
2. `ScoutingPDF.tsx` lignes 141, 195 : SVG `Text` attributs `x/y/fill/textAnchor` passés via spread `as any` (limitation TS de `@react-pdf/renderer` pour les props SVG)
3. `ScoutingPDF.tsx` ligne 348 : `numberOfLines` n'existe pas dans react-pdf `Text` — prop supprimée

## Résultats QA

### BLOCKER — try/finally sur setGenerating
- `generateAndDownloadScoutingPDF` : ✅ `setGenerating(true)` + try/finally `setGenerating(false)`
- `handleScoutingPDF` dans children/page.tsx : ✅ délégation au util qui gère le try/finally

### WARNING — console guards
- `console.error` dans `generateScoutingPDF` : ✅ gardé par `NODE_ENV !== 'production'`

### Validation données insuffisantes
- `if (data.evalCount === 0)` → retourne une erreur sans générer le PDF : ✅

## AC Vérification

| AC | Description | Status |
|----|-------------|--------|
| AC1 | PDF A4 avec header, identité, radar, timeline, tableau | ✅ |
| AC2 | Radar 6 axes labellisés dans le PDF | ✅ |
| AC3 | Timeline 10 dernières évals avec dates X | ✅ |
| AC4 | Photo base64 dans le header ou initiales fallback | ✅ |
| AC5 | Nom fichier `scouting-[slug]-[YYYY-MM-DD].pdf` | ✅ |
| AC6 | Bouton "Génération en cours..." désactivé pendant export | ✅ |
| AC7 | Fallback si 0 évaluations (message d'erreur, pas de PDF) | ✅ |
