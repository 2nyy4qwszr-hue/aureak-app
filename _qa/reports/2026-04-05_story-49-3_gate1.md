# Gate 1 — Code Review — Story 49-3
## Joueurs club non visibles (annuaire)

```yaml
Date       : 2026-04-05
Story      : 49-3
Epic       : 49 — Bugfix batch avril 2026 #2
Agent      : code-reviewer + ux-auditor (Gate 1)
Verdict    : PASS (avec 2 WARNINGs — voir ci-dessous)
```

---

## Fichiers analysés

1. `aureak/packages/api-client/src/admin/child-directory.ts`
2. `aureak/packages/api-client/src/index.ts`
3. `aureak/apps/web/app/(admin)/clubs/[clubId]/page.tsx`

---

## STOP — 5 points critiques (vérification rapide)

| # | Check | Résultat |
|---|-------|----------|
| 1 | Import Supabase direct hors `@aureak/api-client` | OK — `supabase` importé uniquement depuis `../supabase` dans `child-directory.ts` |
| 2 | Style hardcodé (couleur hex, px) | PARTIEL — voir WARNINGs ci-dessous |
| 3 | RLS absent (nouvelle table) | N/A — aucune migration dans cette story |
| 4 | TypeScript `any` nu | OK — aucun `any` non justifié introduit |
| 5 | Acceptance criteria manquants | OK — tous les AC1-AC10 couverts |

---

## Résultats par fichier

### `child-directory.ts` — nouvelle fonction `listChildrenByClubDirectoryId`

**Lignes 706-728**

PASS — Pas de BLOCKER.

Observations positives :
- Fonction correctement typée, retour `{ data: [...]; error: unknown }` cohérent avec les conventions du fichier
- Filtres corrects : `.eq('actif', true)` + `.is('deleted_at', null)` — soft-delete respecté (AC1)
- `.order('display_name', { ascending: true })` — tri stable
- `SELECT` minimal (`id, display_name, statut, niveau_club`) — pas de `SELECT *` (bonne pratique)
- Gestion d'erreur : `if (error) return { data: [], error }` — erreur propagée, pas silencieuse
- Le cast `as Array<{...}>` est explicite et typé — pas de `as any`
- Aucun `console.log` ou `console.error` non gardé

**[INFO]** `child-directory.ts:706` — La fonction ne logue pas l'erreur Supabase en dev. Cohérent avec le pattern du fichier (l'appelant `load()` dans `page.tsx` loggue via console guard). Pas d'action requise.

---

### `index.ts` — export de `listChildrenByClubDirectoryId`

**Ligne 327**

PASS — Export ajouté dans le bloc `from './admin/child-directory'` existant, avec commentaire de story. Pas de doublon. Pas d'oubli de type à exporter (la fonction retourne un type inline anonyme — pas de `export type` nécessaire).

---

### `page.tsx` — modifications UI

#### try/finally

PASS.

- `setLoading(true)` : ligne 413 — reset dans `finally` ligne 442. Conforme.
- `setLoadingLinks(true)` : ligne 414 — reset dans `finally` ligne 443. Conforme.
- `setSaving(true)` : ligne 453 — reset dans `finally` ligne 487. Conforme.
- Le `Promise.all` contient `listChildrenByClubDirectoryId` — la requête annuaire est incluse dans le `finally` existant sans overhead supplémentaire. AC6 et AC7 validés.

#### Console guards

PASS — tous les `console.error` sont guardés `process.env.NODE_ENV !== 'production'` :
- Ligne 440 : `[clubs/detail] load error`
- Ligne 484 : `[clubs/detail] handleSave error`
- Ligne 498 : `[clubs/detail] handleDelete error`
- Ligne 515 : `[clubs/detail] handleToggleActif error`
- Ligne 543 : `[clubs/detail] handleLogoDelete error`
- Ligne 576 : `[clubs/detail] unlinkPlayer error`
- Ligne 596 : `[clubs/detail] unlinkCoach error`

AC10 validé.

#### Routing pattern

PASS — `index.tsx` + `page.tsx` présents dans `clubs/[clubId]/`. Route dynamique correctement configurée.

#### Acceptance criteria — couverture complète

| AC | Vérifié | Fichier:ligne |
|----|---------|---------------|
| AC1 — Section "Joueurs via annuaire" visible | OK | `page.tsx:1011-1036` |
| AC2 — Déduplication (exclusion si déjà lié explicitement) | OK | `page.tsx:389-392` — `useMemo` avec `linkedIds` |
| AC3 — Read-only (pas de bouton Retirer) | OK | `AnnuairePlayerRow` ligne 290-318 — commentaire explicite |
| AC4 — Nom + statut badge + niveauClub | OK | `AnnuairePlayerRow` ligne 300-315 |
| AC5 — Message vide "Aucun joueur lié via l'annuaire." | OK | `page.tsx:1028-1030` |
| AC6 — Inclus dans `Promise.all` | OK | `page.tsx:416-423` |
| AC7 — try/finally respecté | OK | voir section ci-dessus |
| AC8 — Accent violet `#a78bfa` | OK (mais voir WARNING-1) | `page.tsx:295, 1015` |
| AC9 — Compteur post-déduplication | OK | `count={annuairePlayersDeduped.length}` ligne 1014 |
| AC10 — Pas de console non gardé | OK | voir section ci-dessus |

---

## Issues trouvées

### [WARNING-1] — Couleurs hardcodées introduites par story-49-3

**Fichier** : `page.tsx:295` et `page.tsx:1015`

**Valeurs** :
| Fichier:ligne | Valeur trouvée | Token suggéré |
|---------------|----------------|---------------|
| `page.tsx:295` | `backgroundColor: '#a78bfa'` | Token manquant — voir note |
| `page.tsx:1015` | `accent="#a78bfa"` | Token manquant — voir note |

**Note** : La couleur `#a78bfa` (violet) est exigée par AC8 de la story. Elle n'existe pas encore dans `packages/theme/tokens.ts`. La story aurait dû soit (a) ajouter un token `colors.accent.violet` dans `@aureak/theme`, soit (b) utiliser un token existant. Le code respecte l'intention design de la story mais viole la règle "pas de hex hardcodé".

**Statut** : WARNING (non BLOCKER car la couleur est intentionnelle et spécifiée dans l'AC — mais doit être tokenisée dans la prochaine story design ou la story suivante touchant ce fichier).

**Couleurs pre-existantes** (hors scope story-49-3, non bloquantes ici) :
- `#60a5fa` (bleu affiliés) : présent avant cette story — lignes 135, 277, 975, 1006, 1074
- `#4ade80`, `#9E9E9E`, `#3D3420`, `#f87171` : tous pre-existants

### [WARNING-2] — `linksRes.data` — accès sans optional chaining

**Fichier** : `page.tsx:428-429`

```typescript
setCurrentPlayers(linksRes.data.filter(r => r.linkType === 'current'))
setAffiliatedPlayers(linksRes.data.filter(r => r.linkType === 'affiliated'))
```

La fonction `listChildrenOfClub` retourne `{ data: [], error }` en cas d'erreur (jamais `undefined`), donc `linksRes.data` ne peut pas être `null`. Cependant le type déclaré est `{ data: ClubChildLinkRow[]; error: unknown }` — accès sans `?.` est techniquement sûr. Pas de crash possible.

**Statut** : WARNING mineur — cohérent avec le pattern du fichier. `annuaireRes.data` (ligne 438) est identique — même raisonnement.

---

## Logique métier — vérification

- **Déduplication** : `useMemo` à la ligne 389 crée un `Set([...currentIds, ...affiliatedIds])`. Les `currentIds`/`affiliatedIds` sont eux-mêmes des `useMemo` (lignes 384-385). La chaîne de dépendances est correcte — pas de risque de stale.
- **Enum / strings magiques** : `linkType === 'current'` et `linkType === 'affiliated'` correspondent aux valeurs DB définies dans la migration 00051. Le type `ClubChildLinkType` est importé depuis `@aureak/types`. Conforme.
- **Soft-delete** : `listChildrenByClubDirectoryId` filtre `.is('deleted_at', null)` + `.eq('actif', true)`. Conforme.
- **Régression** : l'ajout au `Promise.all` n'affecte pas les autres setters — `setCurrentPlayers`, `setAffiliatedPlayers`, `setCoaches`, `setAllPlayers`, `setAllCoaches` inchangés.

---

## Verdict Gate 1

```
BLOCKERs : 0
WARNINGs  : 2 (WARNING-1 couleur non tokenisée, WARNING-2 accès sans optional chaining)
INFOs     : 1

Gate 1 : PASS
```

**Action requise avant Gate 2** : aucune correction bloquante. Les 2 WARNINGs sont documentés pour traitement dans une story design ultérieure (token `colors.accent.violet`) et sont non-bloquants ici.
