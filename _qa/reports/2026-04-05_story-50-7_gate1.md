# QA Gate 1 — Story 50-7 : Dashboard anomalies inline compactes
Date : 2026-04-05

## Fichiers modifiés
- `aureak/apps/web/app/(admin)/dashboard/page.tsx`

## Checklist BLOCKER (try/finally)

| Pattern | Résultat |
|---------|----------|
| `setResolving(false)` dans `AnomalyModal.handleResolveInModal` | ✅ Dans finally |
| `setLoading(false)` dans `DashboardPage.load` | ✅ Dans finally |
| Pas de `setResolving` inline (ancien `handleResolve` supprimé) | ✅ OK |

## Checklist WARNING (console guards)

| Fichier | Ligne | Guard `NODE_ENV` |
|---------|-------|-----------------|
| `[AnomalyModal] resolve error` | ~1040 | ✅ Oui |
| `[dashboard] fetchActivityFeed error` | ~1321 | ✅ Oui (multiline) |
| `[dashboard] getTopStreakPlayers error` | ~1335 | ✅ Oui (multiline) |
| `[dashboard] Realtime channel error` | ~1374 | ✅ Oui (multiline) |

## Checklist catch silencieux
- Aucun `catch(() => {})` détecté — ✅

## TypeScript
- `npx tsc --noEmit` → **0 erreur** ✅

## AC Coverage

| AC | Description | Statut |
|----|-------------|--------|
| AC1 | Pills flexWrap + colorées par sévérité | ✅ |
| AC2 | Clic pill → modale fixed overlay | ✅ |
| AC3 | Marquer résolu → optimistic filter + toast | ✅ |
| AC4 | Résumé compteurs critique/warning/info | ✅ |
| AC5 | Empty state fond vert + message | ✅ |
| AC6 | Escape + bouton Fermer | ✅ |
| AC7 | Aucune navigation externe | ✅ |

## Bug Hunter — Tests théoriques

| Scénario | Comportement attendu | Couverture code |
|----------|----------------------|-----------------|
| Liste vide (0 anomalie) | Panel affiché, résumé vert, empty state vert léger | ✅ `anomalies.length === 0` branches |
| Erreur résolution | `setError('Erreur lors de la résolution')` dans catch, modal reste ouverte | ✅ |
| Rechargement après résolution | Pas de rechargement — filter optimiste local | ✅ `setAnomalies(prev => prev.filter(...))` |
| Escape sans résolution | `window.removeEventListener` dans cleanup useEffect | ✅ |
| Clic overlay sans résolution | `e.target === e.currentTarget` → `onClose()` | ✅ |

## Design Critic — Conformité tokens
- `colors.overlay.dark`, `colors.light.surface`, `colors.status.success` — tous via tokens ✅
- `radius.badge`, `radius.card`, `radius.button`, `shadows.lg`, `shadows.md` — tous via tokens ✅
- Aucune couleur hardcodée (sauf rgba avec commentaire) ✅

## Résultat Gate 1
**PASS** — Aucun BLOCKER, aucun WARNING non résolu.
