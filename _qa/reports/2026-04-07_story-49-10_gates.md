# QA Report — story-49-10
**Date** : 2026-04-07
**Story** : BUG — `milestones.ts` — typo `getUncelebratedMilestones` + correctif `profiles.id` documenté
**Fichiers analysés** :
- `aureak/packages/api-client/src/gamification/milestones.ts`
- `aureak/packages/api-client/src/index.ts` (ligne ~373)
- `aureak/packages/api-client/src/gamification/admin-profile.ts`
- `aureak/packages/api-client/src/gamification/badges.ts`

---

## Code Review (Gate 1)

### STOP — 5 checks prioritaires

| # | Vérification | Résultat |
|---|-------------|---------|
| 1 | Import Supabase direct hors api-client | ✅ Aucun — tous les accès passent via `../supabase` (interne api-client) |
| 2 | Couleurs/px hardcodés | ✅ Aucun — fichiers purement API, pas de style |
| 3 | RLS absent sur nouvelle table | ✅ N/A — aucune migration SQL dans cette story |
| 4 | TypeScript `any` nu | ✅ Un seul `any` dans milestones.ts ligne 71 : `(data as unknown[])` via `unknown` (justifié — cast JSONB RPC Supabase sans types générés) |
| 5 | Acceptance criteria manquants | ✅ Vérifiés ci-dessous |

### Vérification des Acceptance Criteria

**AC1 — Correctif profiles.id documenté**
- `milestones.ts` ligne 53 : `.eq('user_id', user.id)` — CORRECT
- Grep global `.eq('id', user.id)` dans `api-client/src/` → aucun résultat
- `admin-profile.ts` ligne 15 : `.eq('user_id', adminId)` — CORRECT
- `badges.ts` ligne 96 : `.eq('user_id', childId)` — CORRECT
- **AC1 : PASS**

**AC2 — Typo corrigée dans milestones.ts**
- Déclaration ligne 9 : `export async function getUncelebratedMilestones()` — CORRECT (plus de "d" parasite)
- JSDoc ligne 8 : `/** getUncelebratedMilestones — milestones atteints... */` — CORRECT
- Console guards lignes 20/38 : `[milestones] getUncelebratedMilestones ...` — CORRECT
- **AC2 : PASS**

**AC3 — Export barrel mis à jour**
- `index.ts` ligne 373 : `getUncelebratedMilestones, checkAcademyMilestones, markMilestoneCelebrated` — CORRECT
- **AC3 : PASS**

### Checklist complète

**Architecture** :
- Accès Supabase via `../supabase` (interne au package) : ✅
- Styles : N/A (pas de composant UI) : ✅
- Pas de `any` non justifié : ✅ (l'unique `unknown[]` cast est justifié par JSONB RPC)
- Soft-delete : N/A : ✅
- Enums `@aureak/types` : N/A : ✅

**Logique métier** :
- AC couverts : ✅ (voir ci-dessus)
- Edge cases gérés : ✅ (`if (error) return` + try/catch dans les 3 fonctions de milestones.ts)
- Pas de régression évidente : ✅ (renommage de fonction non consommée par les apps)

**Qualité du code** :
- Console guards : ✅ Tous les `console.error` dans les 4 fichiers sont protégés par `(process.env.NODE_ENV as string) !== 'production'`
- Code mort : INFO — `milestones.ts` ligne 83 : `createdAt: new Date().toISOString()` dans `checkAcademyMilestones` génère une date locale à la place de la vraie date DB (le JSONB RPC ne retourne pas `created_at`). Non bloquant mais comportement potentiellement inattendu.
- Duplication : N/A

**TypeScript** :
- Non-null assertions (`!`) : aucune
- Cast `as` abusifs : `r as Record<string, unknown>` ligne 73 justifié (JSONB unknown)
- Pas de `any` nu : ✅

### Issues trouvées

| ID | Fichier | Ligne | Sévérité | Description |
|----|---------|-------|----------|-------------|
| I-001 | `milestones.ts` | 83 | `[INFO]` | `createdAt: new Date().toISOString()` — valeur synthétique (pas la vraie date DB). La RPC `check_academy_milestones` ne retourne pas `created_at` dans le JSONB. Valeur utilisée uniquement pour l'affichage, non stockée. Acceptable pour l'instant. |

**Aucun BLOCKER. Aucun WARNING. 1 INFO.**

---

## Regression Detector (Gate 2)

### Matrice d'impact

| Fichier modifié | Nature du changement | Stories `done` potentiellement impactées | Type de risque |
|-----------------|---------------------|------------------------------------------|----------------|
| `gamification/milestones.ts` | Renommage `getUnceledbratedMilestones` → `getUncelebratedMilestones` + `.eq('user_id')` déjà corrigé | Aucun consommateur externe identifié dans `apps/` — grep confirme 0 import de l'ancien nom | `[INFO]` renommage safe |
| `api-client/src/index.ts` (ligne 373) | Export barrel mis à jour avec nom corrigé | Aucun fichier `apps/` n'importe `getUncelebratedMilestones` (ni ancien ni nouveau nom) | `[INFO]` safe — pas de consommateur actif |
| `gamification/admin-profile.ts` | `.eq('user_id', adminId)` confirmé correct — aucune modification de signature | story-50-11 (dashboard), story-59-8 | `[INFO]` no change |
| `gamification/badges.ts` | `.eq('user_id', childId)` dans `getAchievementDetails` confirmé correct — aucune modification | story-59-4, story-59-9 | `[INFO]` no change |

### Verdict Régression

Régressions certaines (`[BLOCKER]`) : **0**
Régressions possibles (`[WARNING]`) : **0**
Stories `done` à retester manuellement : **aucune** — les fichiers modifiés n'ont pas de consommateur actif dans `apps/` pour les fonctions renommées. `admin-profile.ts` et `badges.ts` n'ont subi aucune modification de signature.

---

## Bug Hunter — Synthèse finale

### STOP — 5 checks prioritaires

| # | Vérification | Résultat |
|---|-------------|---------|
| 1 | `.data.field` sans optional chaining | ✅ — `data?.` ou `data ??` systématique |
| 2 | Double-soumission possible | ✅ N/A — fichiers API purs, pas de UI |
| 3 | try/catch silencieux | ✅ — tous les catch loguent + retournent `{ error: err }` |
| 4 | Erreur Supabase ignorée | ✅ — `error` toujours déstructuré et vérifié |
| 5 | Mutation sans invalidation | ✅ N/A — pas de TanStack Query dans ces fichiers |

### Edge cases & bugs potentiels

**milestones.ts — checkAcademyMilestones**
- `const { data: { user } } = await supabase.auth.getUser()` ligne 47 : déstructuration directe sans vérification d'erreur sur `getUser()`. Si le réseau est coupé, `user` peut être `null` → ligne suivante `if (!user) return` gère le cas null. SAFE.
- `profile.tenant_id` ligne 62 : utilisé directement sans optional chaining. Cependant la garde `if (profileErr || !profile)` ligne 56 stoppe l'exécution si `profile` est falsy. SAFE.

**admin-profile.ts — getAdminActivityStats**
- Toutes les `count` requêtes sont non-bloquantes (erreurs loguées, fallback `?? 0`). SAFE.
- `xp_ledger` requête avec `.eq('child_id', adminId)` — sémantique questionnable (un admin ne devrait pas avoir de `child_id` dans `xp_ledger`), mais hors scope de cette story (comportement inchangé).

**badges.ts — getAchievementDetails**
- `Promise.all` ligne 95 : si l'un des deux appels lève une exception non-Promise (peu probable avec Supabase), le catch global l'attrape. SAFE.
- `playerResult.data?.display_name ?? 'Joueur'` : optional chaining + fallback. SAFE.

### Warnings non résolus des agents précédents

| Agent | Warning | Résolu dans cette story? | Action requise |
|-------|---------|--------------------------|----------------|
| bug-crawler 2026-04-06 | B-CRAWLER-05 : `profiles.id` inexistant dans `milestones.ts:52` | ✅ OUI — `.eq('user_id', user.id)` confirmé | Marquer B-CRAWLER-05 résolu dans summary.md |
| — | Typo `getUnceledbratedMilestones` dans index.ts | ✅ OUI — renommé en `getUncelebratedMilestones` | — |

---

## Verdict Final Story 49-10

Gate 1 (Code Review) : ✅ PASS
Gate 2 (Régressions + Bugs) : ✅ PASS
BLOCKERs : 0
WARNINGs : 0
INFOs : 1 (createdAt synthétique dans checkAcademyMilestones — non bloquant)

**Recommandation : PRÊT POUR PRODUCTION**

> Le BLOCKER B-CRAWLER-05 (`profiles.id` inexistant) est résolu dans cette story. Mettre à jour `_qa/summary.md` en marquant B-CRAWLER-05 comme résolu.
