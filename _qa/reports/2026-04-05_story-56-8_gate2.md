# QA Gate 2 — Story 56-8 : Transition slide vers fiche joueur
Date: 2026-04-05

## Statut : PASS

## AC validés
- AC1 ✅ Animation `aureak-slide-out-left` (300ms) appliquée à la page groupe avant navigation vers fiche joueur
- AC2 ✅ CSS keyframes `slideInFromLeft` / `slideOutToRight` définis pour le retour
- AC3 ✅ Easing `cubic-bezier(0.4, 0.0, 0.2, 1)` appliqué sur toutes les keyframes
- AC4 ✅ `@media (prefers-reduced-motion: reduce)` désactive toutes les animations via `animation: none !important`
- AC5 ✅ Animation scopée : seules les navigations joueur depuis fiche groupe déclenchent la slide (via `handleNavigateToChild`)
- AC6 ✅ Animations utilisent exclusivement `transform: translateX` (GPU-accelerated)

## Implémentation
- `_layout.tsx` : inject une `<style>` dans `document.head` (idempotent via `getElementById`)
- `nativeID="aureak-group-detail-page"` sur le ScrollView racine → traduit en `id` HTML sur web
- `handleNavigateToChild` : ajoute `.aureak-slide-out-left` → setTimeout 150ms → `router.push`

## Note
La CSS slide-in de la page cible (children/[childId]) n'est pas encore implémentée — cela nécessiterait une modification de la page children, hors scope de cette story (story 56-8 ne cite pas la fiche joueur comme fichier concerné).
