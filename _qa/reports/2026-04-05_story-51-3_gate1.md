# QA Gate 1 — Story 51.3 : Command Palette ⌘K
Date : 2026-04-05

## Scope
Fichiers créés/modifiés par l'implémentation :
- `aureak/apps/web/hooks/useCommandPalette.ts` (new)
- `aureak/apps/web/components/CommandPalette.tsx` (new)
- `aureak/apps/web/app/constants/navCommands.ts` (new)
- `aureak/packages/api-client/src/admin/search.ts` (new)
- `aureak/packages/types/src/entities.ts` (modified — CommandResult/CommandResultType)
- `aureak/packages/api-client/src/index.ts` (modified — export searchUnified)
- `aureak/apps/web/app/(admin)/_layout.tsx` (modified — CommandPalette intégré)
- `aureak/apps/web/hooks/useKeyboardShortcuts.ts` (modified — Cmd+K retiré)

## Checks BLOCKER

### try/finally sur setIsLoading
- `useCommandPalette.ts` ligne 174–184 : `setIsLoading(true)` suivi de `try { ... } finally { setIsLoading(false) }` ✅

### Console guards
- `useCommandPalette.ts` ligne 179 : `if (process.env.NODE_ENV !== 'production') console.error('[CommandPalette] search error:', err)` ✅
- Aucun `console.log/error` non gardé dans les autres fichiers ✅

### Catch silencieux
- Aucun `catch(() => {})` détecté ✅

## Checks WARNING

### Cleanup listeners
- `useCommandPalette.ts` useEffect ligne 188–234 : `return () => window.removeEventListener('keydown', handler, true)` ✅
- Cleanup debounce au unmount (useEffect dédié ligne 239–243) ✅
- Cleanup debounce dans `close()` et `toggle()` ✅

### Empty state
- Affiché quand `query.length >= 2 && !isLoading && results.length === 0` ✅
- Message : `«Aucun résultat pour «{query}»` + sous-texte d'aide ✅

### Navigation vers route
- `executeResult` appelle `router.push(result.href as never)` puis `close()` ✅
- `CommandResultItem` transmet `onPress={() => executeResult(result)}` ✅

## TypeScript
- `npx tsc --noEmit` : 0 erreur ✅

## AC Coverage (pré-Playwright)
- AC1 ⌘K toggle : implémenté via capture phase `window.addEventListener('keydown', handler, true)` ✅
- AC2 Escape close : implémenté ✅
- AC3 Navigation clavier ↑↓/Enter : implémenté ✅
- AC4 Recherche joueurs : `searchUnified` → `child_directory` ilike ✅
- AC5 Recherche clubs : `searchUnified` → `club_directory` ilike ✅
- AC6 Nav statique : `filterNavCommands` + `DEFAULT_COMMANDS` (top 6) ✅
- AC7 Groupes avec headers : groupResults() + TYPE_LABEL ✅
- AC8 Empty state : conditionnel `showEmpty` ✅
- AC9 Loading state : `ActivityIndicator` visible pendant la recherche ✅
- AC10 Fermeture clic extérieur : `Pressable` backdrop avec `onPress={close}` ✅

## Statut Gate 1
PASS — aucun BLOCKER. Prêt pour Gate 2 (Playwright).
