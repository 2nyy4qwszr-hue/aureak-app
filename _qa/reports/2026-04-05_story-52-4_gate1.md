date: 2026-04-05
story: 52-4
gate: 1
status: pass

# Gate 1 — Static QA

## Fichiers modifiés
- `aureak/apps/web/app/(admin)/children/index.tsx` — toggle vue galerie/liste

## Checks BLOCKER

### Try/finally
- `setLoading(false)` ligne 963 : dans `finally` — PASS (pre-existant)
- `setImporting(false)` ligne 1055 : dans `finally` — PASS (pre-existant)
- `setViewMode` : synchrone, pas de state setter de chargement — N/A

### Console guards
- grep console.* | grep -v NODE_ENV → 0 résultat — PASS

### Couleurs hardcodées ajoutées
- Aucune couleur hex hardcodée dans le nouveau code — PASS
  (couleurs toggle : colors.accent.gold, colors.border.light, colors.light.surface)

## Checks AC

- AC1 : Bouton toggle double-icône (⊞/☰), actif = backgroundColor gold + texte blanc — PASS
- AC2 : gridGalerie style = flexDirection row, flexWrap wrap, gap space.md — PASS
        (Desktop breakpoints non gérés en RN sans media queries — acceptable, flexWrap auto-flow)
- AC3 : Vue liste = PremiumJoueurCard (comportement pré-story-52 inchangé) — PASS
- AC4 : localStorage via `loadViewMode()` + useEffect persist — PASS
- AC5 : Défaut 'liste' si clé absente — PASS
- AC6 : Compteur `{total} joueurs` dans le header — présent dans les deux modes — PASS
- AC7 : PAGE_SIZE = viewMode === 'galerie' ? 48 : 50 — PASS

## Dépendances useCallback
- `load` inclut `PAGE_SIZE` dans le tableau de dépendances — PASS
- `setPage(0)` déclenché sur changement de viewMode — PASS

## TypeScript
`npx tsc --noEmit` → 0 erreur
