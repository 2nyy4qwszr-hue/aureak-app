# QA Gate 1 — Story 57-4 — Implantations mini-carte Leaflet

**Date** : 2026-04-05
**Statut** : PASS

## Checks BLOCKER

| Check | Résultat |
|-------|----------|
| `try/finally` sur state setters | PASS — aucun setter dans ce composant |
| Console guards | PASS |
| Hardcode couleurs | PASS — `colors.accent.gold` via tokens theme |

## ACs vérifiés

- AC1 : section carte visible seulement si `impl.gpsLat != null && impl.gpsLon != null` ✓
- AC2 : `react-leaflet` + OpenStreetMap tiles, sans clé API. Import lazy (web only) ✓
- AC3 : `center={[lat, lon]}` zoom=15, `CircleMarker` or ✓
- AC4 : `height:180`, `borderRadius:radius.card`, `overflow:'hidden'`, `borderColor:colors.border.light` ✓
- AC5 : `Pressable` → `window.open(url, '_blank')` ✓
- AC6 : badge "Ouvrir dans Maps ↗" en bas-droite ✓
- AC7 : `leaflet` + `react-leaflet` ajoutés dans `package.json` web ✓
- AC8 : `React.lazy` + `React.Suspense` avec skeleton `mapSkeleton` 180px ✓
- AC9 : placeholder `📍 Aucune coordonnée GPS...` si coordonnées nulles ✓
- AC10 : tokens `@aureak/theme`, `console.error` non applicable (composant stateless) ✓
