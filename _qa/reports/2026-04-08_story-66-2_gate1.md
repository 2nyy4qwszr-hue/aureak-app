# Gate 1 Report — Story 66.2
**Date** : 2026-04-08
**Story** : UX — Sidebar admin : ajouter "Stages" sous Évènements avec badge count actifs
**Fichiers analysés** : `aureak/apps/web/app/(admin)/_layout.tsx`

---

## Résumé

| Catégorie     | Résultat |
|---------------|----------|
| BLOCKERs      | 0        |
| WARNINGs      | 0        |
| INFOs         | 1        |
| Corrections   | 0        |

**Verdict : PASS**

---

## Checklist rapide (5 points BLOCKER)

1. Import Supabase direct hors api-client : **PASS** — `listStages` importé depuis `@aureak/api-client`
2. Styles hardcodés : **PASS** — badge utilise `colors.accent.gold` depuis `@aureak/theme` ; les `rgba(...)` présents sont des patterns pré-existants d'autres stories
3. RLS absent : **PASS** — aucune migration, RLS déjà en place sur `stages`
4. TypeScript `any` nu : **PASS** — aucun `any` introduit
5. Acceptance criteria couverts : **PASS** — tous les 8 AC vérifiés (voir détail ci-dessous)

---

## Vérification des Acceptance Criteria

| AC | Description | Statut |
|----|-------------|--------|
| AC1 | Item "Stages" avec route `/stages` et `CalendarIcon` dans NAV_GROUPS Évènements | ✅ (ligne 128) |
| AC2 | `listStages({ status: 'en_cours' })` appelé au mount, résultat stocké dans `stagesActifsCount` | ✅ (lignes 369-391) |
| AC3 | Badge doré `NavBadge` si `stagesActifsCount > 0` | ✅ (lignes 672-674, 694-696) |
| AC4 | Si count=0 ou erreur API → pas de badge, item toujours visible | ✅ (catch silencieux, init à 0) |
| AC5 | `try/finally` avec `stagesLoading` | ✅ (lignes 376, 383-385) |
| AC6 | Mode collapsed : icône seule + badge en superposition | ✅ (lignes 658-675) |
| AC7 | Détection active : `pathname === '/stages'` ou `pathname.startsWith('/stages/')` | ✅ (ligne 621 — pattern générique) |
| AC8 | Aucune migration SQL requise | ✅ |

---

## Qualité du code

### try/finally
Pattern correct avec flag `cancelled` :
- `setStagesLoading(true)` avant le try
- `finally { if (!cancelled) setStagesLoading(false) }` — CONFORME

### console guards
Tous les `console.error` ajoutés par cette story sont guardés :
- Ligne 381 : `if (process.env.NODE_ENV !== 'production') console.error('[AdminLayout] listStages error:', err)` — CONFORME

### Architecture
- Import `listStages` depuis `@aureak/api-client` (index.ts l.24 → admin/stages.ts l.114) — CONFORME
- `'en_cours'` est une valeur valide de `StageStatus` (entities.ts l.558) — CONFORME
- `NavBadge` composant existant réutilisé sans modification — CONFORME
- `CalendarIcon` déjà importé — aucun import supplémentaire nécessaire

---

## INFO

- **INFO** — Deux fonctions `listStages` existent dans api-client : `academy/academyStatus.ts` (retourne `{ data, error }`) et `admin/stages.ts` (retourne `StageWithMeta[]`). Seule la version `admin/stages.ts` est exportée depuis `index.ts`, ce qui est correct. Pas de risque de confusion à runtime, mais la fonction dans `academyStatus.ts` est désormais orpheline (non-exportée). Hors scope de cette story.

---

## Corrections apportées

Aucune correction nécessaire. L'implémentation est conforme à toutes les règles.
