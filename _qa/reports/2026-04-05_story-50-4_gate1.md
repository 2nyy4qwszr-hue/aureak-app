# QA Gate 1 — Story 50.4 — Dashboard ImplantationCard terrain premium
date: 2026-04-05
status: pass

## Fichiers modifiés
- `aureak/apps/web/app/(admin)/dashboard/page.tsx`

## Checklist BLOCKER

- [x] `setLoading(false)` dans `finally` (ligne 862) — OK
- [x] `setLoadingUpcoming` dans `finally` — OK (pré-existant)
- [x] `setLoadingCounts` dans `finally` — OK (pré-existant)
- [x] Aucun console non-guardé — tous console.error wrappés `process.env.NODE_ENV !== 'production'`
- [x] Aucun catch silencieux — chaque catch logue l'erreur

## Checklist ACs

- [x] AC1 — Header 80px avec `linear-gradient(135deg, #1B4332 0%, #2D6A4F 50%, #40916C 100%)`
- [x] AC2 — Badge or nombre de groupes (children_count absent de ImplantationStats → fallback groups.length)
- [x] AC3 — Chips groupes scrollables (overflow-x: auto, groups-scroll className, "Aucun groupe" si vide)
- [x] AC4 — Nom implantation Montserrat 700 taille 16 blanc avec text-shadow
- [x] AC5 — ProgressBar Présence + Maîtrise préservées, footer "Séances clôturées" préservé
- [x] AC6 — CSS hover: `.aureak-card:hover .implant-card-header { box-shadow: 0 4px 20px rgba(64,145,108,0.3) }`
- [x] AC7 — Prop `groups?: { id: string; name: string }[]` ajoutée, chargement parallèle via `listGroupsByImplantation`

## Tokens theme utilisés
- `colors.accent.gold` — badge fond
- `colors.light.muted` — chips fond
- `colors.text.dark` — chips texte
- `colors.text.muted` — label "Aucun groupe" + ProgressBar
- `radius.badge` — chips + badge border-radius
- `shadows.md` — hover card (pré-existant)

## Warnings
- W1: `rgba(64,145,108,0.3)` hardcodé dans le glow hover — token `colors.status.presentGlow` absent de @aureak/theme (conforme story, valeur spécifique terrain)
- W2: `#1B4332 / #2D6A4F / #40916C` hardcodés dans le gradient header — idem TERRAIN_GRADIENT existant dans le fichier, cohérence voulue pour le vert terrain spécifique
- W3: `'#FFFFFF'` et `'#1A1A1A'` couleurs badge hardcodées — tokens colors.text.primary/#FFFFFF non mappés exactement
