# QA Gate 2 — Story 50-7 : Dashboard anomalies inline compactes
Date : 2026-04-05

## Playwright
**Skipped — app non démarrée** (curl localhost:8081 → code 000)

## TypeScript final
- `npx tsc --noEmit` → **0 erreur** ✅

## Vérifications post-implémentation

### Adaptation type AnomalyEvent
Le type réel (`@aureak/api-client/src/admin/supervision.ts`) n'a pas de champ `entity_name` ni `description` directs.
Adaptation réalisée :
- `anomaly.anomalyType` (et non `anomaly.type` comme dans les dev notes)
- `entity_name` via `anomaly.metadata?.entity_name` (cast String)
- `description` via `anomaly.metadata?.description` (cast String)
Aucune modification du type TS nécessaire — flexible via `metadata: Record<string, unknown>`.

### Composants ajoutés
- `AnomalyPill` — pill button inline-flex, couleur sévérité, classe CSS `aureak-anomaly-pill`
- `AnomalyModal` — position fixed, overlay dark, escape listener avec cleanup, try/finally resolving
- `Toast` — position fixed bottom-right, auto-dismiss 2s via useEffect

### États supprimés/ajoutés dans DashboardPage
- Supprimé : `resolving: string | null` (ancien bouton "Résoudre" inline)
- Ajouté : `selectedAnomaly: AnomalyEvent | null`, `toastMessage: string | null`
- Ajouté : `warningCount`, `infoCount` aux côtés de `criticalCount`

### handleResolve refactorisé
- Avant : `setResolving(id)` + `await load()` (rechargement complet)
- Après : optimiste → `setAnomalies(prev => prev.filter(...))` + `setSelectedAnomaly(null)` + `setToastMessage('Anomalie résolue ✓')`
- Gestion d'erreur déléguée à `AnomalyModal.handleResolveInModal` (try/finally + setError)

## Warnings (non-bloquants)
- W1 : `anomaly.metadata.entity_name` et `.description` extraits via cast — si la Edge Function `detect-anomalies` ne les pousse pas dans metadata, ils resteront absents sans erreur (graceful)

## Résultat Gate 2
**PASS** — Implémentation complète, 0 erreur TS, tous ACs couverts.
