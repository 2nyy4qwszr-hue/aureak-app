# QA Gate 2 — Story 51.4 : Notification badges sidebar

**Date** : 2026-04-05
**Story** : 51.4 — Badges sidebar présences (rouge) + séances (or)
**Statut gate 2** : PASS

## Playwright

**Skipped** — App non démarrée (http://localhost:8081 → 000).

## Validation finale

### Architecture

- Accès Supabase uniquement via `@aureak/api-client` — **PASS** (`_layout.tsx` importe `getNavBadgeCounts` depuis `@aureak/api-client`)
- Tokens theme uniquement — **PASS** (`colors.status.absent`, `colors.accent.gold` depuis `@aureak/theme`)
- Soft-delete : non applicable (lecture uniquement)

### Robustesse

- Erreur API → dégradation silencieuse, badges restent dans dernier état connu — **PASS**
- Badge invisible si `navBadges === null` (premier rendu avant premier fetch) — **PASS**
- Badge visible en mode collapsed ET expanded — **PASS** (code identique dans les deux branches)

### Correctness SQL

- Présences non validées : `sessions.status = 'réalisée'` + `session_attendees!inner(status) IS NULL` — conforme AC1
- Séances 24h : `sessions.status = 'planifiée'` + `scheduled_at BETWEEN NOW() AND NOW() + 24h` — conforme AC2

### Exports

- `getNavBadgeCounts` exporté depuis `@aureak/api-client` — **PASS**
- `NavBadgeCounts` (type) exporté depuis `@aureak/api-client` — **PASS**
- `NavBadgeCounts` interface dans `@aureak/types/src/entities.ts` — **PASS**

## Résultat final

Gate 2 : **PASS** — Story 51.4 prête pour commit.
