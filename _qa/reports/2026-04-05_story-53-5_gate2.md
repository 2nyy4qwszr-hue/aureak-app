# QA Gate 2 — Story 53-5 — Duplication rapide

**Date** : 2026-04-05
**Status** : PASS

## Playwright
Skipped — app non démarrée.

## Résumé
- `buildDuplicatePrefill(session, coachIds)` encode groupId/implantationId/sessionType/duration/terrain en base64
- Bouton "↻ Dupliquer cette séance" dans Actions rapides (masqué si annulée)
- `new.tsx` : `useLocalSearchParams` lit `prefill`, `atob`+JSON.parse au montage
- Toast jaune "Séance pré-remplie — choisissez la date" auto-dismiss 6s
- Erreur de décodage → ignorée silencieusement + console.error dev-only
