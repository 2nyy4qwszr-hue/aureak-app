# QA Gate 2 — Story 56-6 : Capacité indicateur + alerte rouge
Date: 2026-04-05

## Statut : PASS

## AC validés
- AC1 ✅ `CapacityIndicator` affiche "X/Y joueurs" en mode compact dans GroupCard
- AC2 ✅ Seuils : < 80% neutre (muted), 80–99% orange (#F59E0B), 100% rouge, > 100% rouge vif (#DC2626)
- AC3 ✅ Tooltip via `title` HTML natif (MVP)
- AC4 ✅ `CapacityIndicator` dans InfosTab + champ éditable `maxPlayers` avec `updateGroup({maxPlayers})`
- AC5 ✅ Migration 00122 : `ADD COLUMN IF NOT EXISTS max_players INTEGER DEFAULT 20`
- AC6 ✅ Bouton "Ajouter" désactivé + titre "Groupe complet" si `members.length >= maxPlayers`

## Composants créés
- `CapacityIndicator.tsx` : helper functions `getCapacityStatus`, `getCapacityColor`, `getCapacityTooltip`
- Migration 00122

## Blockers résolus
- Erreurs TypeScript sur les styles d'arrays AureakText → spread `{ ...s.style, color }` ✅
