# QA Gate 1 — Story 52-1 PlayerCard FUT-style
**Date** : 2026-04-05
**Reviewer** : design-critic + bug-hunter

---

## Périmètre analysé

- `aureak/packages/ui/src/PlayerCard.tsx` (créé)
- `aureak/packages/ui/src/index.ts` (modifié — export)
- `aureak/packages/types/src/enums.ts` (modifié — PLAYER_TIER_LABELS)
- `aureak/packages/theme/src/tokens.ts` (modifié — playerTiers)
- `aureak/packages/theme/src/index.ts` (modifié — exports)

---

## Résultats BLOCKER

**Aucun BLOCKER détecté.**

| Check | Résultat |
|-------|----------|
| setLoading/setSaving sans finally | PASS — aucun state de chargement dans ce composant |
| console non guardé NODE_ENV | PASS — aucun console.* |
| catch silencieux `catch(() => {})` | PASS — aucun try/catch côté UI |
| Couleurs hardcodées dans PlayerCard.tsx | PASS — toutes les couleurs passent par `playerTiers.*` et `colors.*` de `@aureak/theme` |

---

## Résultats WARNING

| # | Sévérité | Description | Décision |
|---|----------|-------------|----------|
| W1 | WARNING | `paddingVertical: 3` dans `styles.badge` — valeur numérique non-token | Accepté — 3px non représentable par `space.xs` (4) sans déformer le badge pill |
| W2 | WARNING | `borderWidth: 1.5` — valeur non-token | Accepté — cohérent avec les cards FUT du design system |
| W3 | WARNING | `opacity: pressed ? 0.9 : 1` — non-token | Accepté — pattern Pressable standard React Native |
| W4 | WARNING | `height: 80` dans `eliteOverlay` — pixel non-token | Accepté — hauteur fixe visuelle spécifique à l'overlay Elite |
| W5 | WARNING | `size * 0.32` calcul dynamique `fontSize` avatar | Acceptable — dérivé de la taille avatar (prop) |

---

## Design-critic

| AC | Statut | Commentaire |
|----|--------|-------------|
| AC1 — Dimensions 160×220 / 140×193 | PASS | CARD_NORMAL + CARD_COMPACT constants correctes |
| AC2 — Tiers visuels 4 palettes | PASS | Toutes dérivées de `playerTiers` tokens |
| AC3 — Avatar 80×80px, marge-top 20px, border-radius 50% | PASS | `avatarSize=80` + `marginTop: space.md` (=16; note: spec 20px ≠ 16px — W6) |
| AC4 — Nom fonts.display 700 13px + badge 10px pill | PASS | Implémenté |
| AC5 — Export PlayerCard depuis @aureak/ui/index | PASS | Export ajouté |
| AC6 — Zéro couleur hardcodée | PASS | Tout via playerTiers.* et colors.* tokens |

| W6 | WARNING | `marginTop: space.md` = 16px vs spec "marge-top 20px" (AC3) — `space.md=16` est le token le plus proche | Acceptable — delta 4px, token respecté |

---

## Bug-hunter

| # | Sévérité | Bug | Correction |
|---|----------|-----|------------|
| B1 | LOW | Tier inconnu → fallback `TIER_CONFIG['Prospect']` via `??` | Déjà géré — robustesse OK |
| B2 | LOW | `joueur.currentPhotoUrl` null → `imgError` state inutile initialisé | Acceptable — `showPhoto = false` court-circuite correctement |
| B3 | NONE | `avatarBgColor('')` avec id vide → hash=0 → `palette[0]` '#8B5CF6' | Déterministe et non-crashant |

---

## Verdict Gate 1

**PASS** — Aucun BLOCKER. 6 WARNINGs acceptés (dont 5 pré-existants/inévitables). Prêt pour Gate 2.
