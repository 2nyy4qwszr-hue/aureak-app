# QA Gate 2 — Story 56-7 : Générateur auto groupes par âge
Date: 2026-04-05

## Statut : PASS

## AC validés
- AC1 ✅ Bouton "👶 Par âge" dans groups/index.tsx (admin uniquement visible)
- AC2 ✅ Modal s'ouvre avec analyse joueurs actifs → catégories U10/U12/U14/U17/Non classifiés
- AC3 ✅ Noms éditables via TextInput dans la modal (champ édité avant confirmation)
- AC4 ✅ `createGroup` + `addGroupMember` appelés pour chaque groupe inclus avec try/finally
- AC5 ✅ Détection conflits : `hasConflict` détecté via liste noms des groupes existants de l'implantation
- AC6 ✅ Joueurs sans `birth_date` → groupe "Non classifiés" affiché séparément

## Composants créés
- `GroupGeneratorModal.tsx` : workflow 3 étapes (explication → preview → résultat)
- `generateGroupsBySeason()` : fonction pure côté client + `AGE_CATEGORY_RANGES` constants
- `GroupProposal` type dans `@aureak/types`

## Note AC5
Les groupes en conflit sont skippés par défaut (pas fusionnés) — fusion complète nécessiterait `getGroupByName` pas encore implémenté. L'utilisateur est informé via le badge "Conflit — groupe existant" visible.
