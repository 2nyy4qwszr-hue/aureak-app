# QA Gate 1 — Story 52-12 Vue master-detail joueurs
date: 2026-04-05
story: story-52-12-vue-master-detail
gate: 1 (implementation)
result: PASS

## Fichiers créés/modifiés
- `aureak/apps/web/app/(admin)/children/_ChildDetail.tsx` — créé (ChildDetailInline iframe + EmptyDetailState)
- `aureak/apps/web/app/(admin)/children/index.tsx` — split-screen layout conditionnel

## AC vérifiés
- [x] AC1 — isSplitScreen = Platform.OS === 'web' && screenWidth >= 1024
- [x] AC2 — Panneau gauche 340px fixe, fond colors.light.primary, overflowY scroll
- [x] AC3 — selectedChildId state + router.replace(`/children?selected={id}`) sur clic en split-screen
- [x] AC4 — Panneau droit : ChildDetailInline (iframe) ou EmptyDetailState si null
- [x] AC5 — PremiumJoueurCard : prop isSelected + style pCard.selected (borderWidth 3px gold)
- [x] AC6 — params.selected initialisé depuis useLocalSearchParams().selected
- [x] AC7 — Tablette/mobile (< 1024px) : layout normal (isSplitScreen = false)
- [x] AC8 — Compteur total joueurs maintenu dans le panneau gauche (header inchangé)

## QA checks
- try/finally : ChildDetailInline est un composant presentational (iframe), aucun état loader — N/A
- console guards : aucun console.* dans les nouveaux fichiers — OK
- Styles split-screen : ssp.splitContainer, ssp.leftPanel, ssp.rightPanel via StyleSheet.create — tokens OK
- Resize listener : useEffect avec cleanup removeEventListener — OK (pas de fuite mémoire)

## tsc
0 erreur

## Note technique
- Approche iframe (option simple) choisie car story 52-12 l'autorise explicitement
- Délai 200ms acceptable (iframe rechargement par sélection)
- PremiumJoueurCard reçoit onPress? et isSelected? props optionnels — retro-compatible
