# QA Gate 2 — Story 50.4 — Dashboard ImplantationCard terrain premium
date: 2026-04-05
status: pass

## TypeScript
- `npx tsc --noEmit` → 0 erreur

## Architecture
- [x] Import `listGroupsByImplantation` via `@aureak/api-client` (règle 1 — pas d'accès direct Supabase)
- [x] Styles via tokens `@aureak/theme` (colors, radius, shadows) — exceptions documentées en Gate 1 W1-W3
- [x] Routing Expo Router — aucun nouveau fichier de route créé

## Design-critic gradient terrain
- Gradient `linear-gradient(135deg, #1B4332 0%, #2D6A4F 50%, #40916C 100%)` conforme AC1
- Distinct de `TERRAIN_GRADIENT` existant (`linear-gradient(135deg, #1a472a 0%, #2d6a4f 60%, #1a472a 100%)`) — couleurs légèrement différentes, les deux sont du vert terrain riche
- Nuance : la story spécifie explicitement les trois stops hexadécimaux — OK de les hardcoder

## Régression
- [x] `ImplantationCardHeader` (ancienne fonction) supprimée — aucune autre référence dans le fichier
- [x] ProgressBars Présence + Maîtrise inchangées fonctionnellement
- [x] Footer "Séances clôturées" conservé
- [x] Fallback sans régression si `groups` non fourni (`groups != null` check)

## Données
- `listGroupsByImplantation` appelée en parallèle pour chaque implantation après `getImplantationStats`
- Erreur individuelle catchée par implantation → fallback `[]` (pas de crash global)
- `implantationGroups` state initialisé `{}` → `groups` prop = `undefined` jusqu'au chargement (pas de régression chips)

## Playwright
- App non démarrée — test Playwright skipped
