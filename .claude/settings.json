{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git push*)",
            "command": "CMD=$(jq -r '.tool_input.command // \"\"'); case \"$CMD\" in *\"--help\"*|*\"--dry-run\"*) exit 0 ;; esac; echo '{\"systemMessage\":\"⚠️ Avant de push : as-tu lancé /review-pr ? (branche courante vs origin/main) — review locale gratuite via sous-agents Claude Code.\"}'"
          }
        ]
      }
    ]
  }
}
